const statuses = {
  SUCCESS: 'SUCCESS',
  ERROR: 'ERROR',
}

const languages = {
  EN: 'EN',
}

const senderTypes = {
  PUBLISHER: 'PUBLISHER',
  SUBSCRIBER: 'SUBSCRIBER',
  RANDOM: 'RANDOM',
}

module.exports = { statuses, languages, senderTypes }
