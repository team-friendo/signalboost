#!/usr/bin/env bash

echo "--- running signalc tests..."

docker-compose -f docker-compose-test.yml run --entrypoint 'gradle --console=plain test' signalc

docker ps --filter name=test_ -aq | xargs -I container_id docker rm -f container_id
