#!/usr/bin/env bash

if [ -z $CHANNEL_PHONE_NUMBER ];then
  echo "--- ERROR: no CHANNEL_PHONE_NUMBER found."
  exit 1
fi

echo "> Running signal-cli in daemon mode for channel $CHANNEL_PHONE_NUMBER"

signal-cli -u $CHANNEL_PHONE_NUMBER daemon --system
